'use client';
import Link from 'next/link';
import { useEffect, useState } from 'react';
function token(){return localStorage.getItem('printer-token');} function money(v){return `₹${Number(v||0).toFixed(2)}`;}
export default function Dashboard(){const[jobs,setJobs]=useState([]);const[msg,setMsg]=useState('Loading...');useEffect(()=>{fetch('/api/jobs',{headers:{'x-user':token()||''}}).then(r=>r.json().then(d=>{if(!r.ok)setMsg(d.error);else{setJobs(d.jobs||[]);setMsg('');}}));},[]);return <main className="shell"><nav className="topnav"><Link href="/">Home</Link><Link href="/upload">Upload</Link></nav><section className="card"><h1>User dashboard</h1><p className="hint">Only your own orders are returned by the API.</p>{msg&&<p className="hint">{msg}</p>}<div className="table-wrap"><table><thead><tr><th>Order</th><th>Document</th><th>Pages</th><th>Copies</th><th>Amount</th><th>Order Status</th><th>Payment</th><th>Print</th></tr></thead><tbody>{jobs.map(job=><tr key={job.id}><td><Link href={`/jobs/${job.id}`}>#{job.id}</Link></td><td>{job.originalFilename}</td><td>{job.pageCount}</td><td>{job.copies}</td><td>{money(job.amountRupees)}</td><td><span className={`pill ${job.status?.toLowerCase().replaceAll(' ','-')}`}>{job.status}</span></td><td>{job.paymentStatus}</td><td>{job.printStatus}</td></tr>)}{!jobs.length&&<tr><td colSpan="8">No jobs yet.</td></tr>}</tbody></table></div></section></main>}
